apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: ppl-app

resources:
  - ../../base

images:
  - name: PLACEHOLDER_IMAGE
    newName: docker.io/stevensetiawankuliah/ppl-c-steven-app
    newTag: sha-5d7d8c9

patches:
  - target:
      kind: Deployment
      name: ppl-app
    patch: |-
      - op: replace
        path: /spec/template/spec/containers/0/env/0/value
        value: "v1.0.0"
