apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment-blue.yaml
  - deployment-green.yaml
  - service.yaml
  - networkpolicy.yaml

commonLabels:
  app: ppl-app
  managed-by: argocd
